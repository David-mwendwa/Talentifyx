# Base URL of the Talentifyx API, including /api/v1.
# Local development leaves this unset: Vite proxies /api to the backend port.
# On Netlify set it to the Render service URL, e.g.
# VITE_API_URL=https://talentifyx-api.onrender.com/api/v1
VITE_API_URL=
